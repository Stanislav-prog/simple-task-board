# Technical stack

## Verified

**Verified:** The only technology named by the existing project documentation is the browser's `localStorage` API as the persistence boundary.

**Source:** README.md.

## Unknown

**Unknown:** No framework, language, package manager, dependency set, build tool, bundler, test runner, formatter, linter, browser support policy, or deployment target can be verified. No package or configuration files were present during repository inspection.

**Source:** repository inspection.

## Policy

**Planned:** Future implementation documentation should add concrete stack details only after they are present in repository files or approved by a separate task.

**Source:** approved task contract.
