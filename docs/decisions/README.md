# Architecture decisions

**Verified:** This directory records approved decisions. It currently contains no decision records beyond this index.

**Source:** repository inspection; approved task contract.

## Current decisions

### Local browser persistence

**Verified:** The project boundary uses browser `localStorage` for task persistence and does not include a backend.

**Source:** README.md.

### Stable statuses

**Verified:** The canonical status vocabulary is `todo`, `in_progress`, and `done`.

**Source:** approved task contract.

## Unrecorded decisions

**Unknown:** Framework, build tooling, dependencies, concrete task schema, validation, ordering, serialization, storage key, backend, authentication, and deployment decisions have not been made or are not present in the repository.

**Source:** repository inspection; approved task contract.
